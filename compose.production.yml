# Standalone configuration: do not combine with docker-compose.yml.
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ravex
      POSTGRES_PASSWORD_FILE: /run/secrets/postgres_password
    volumes:
      - production_postgres:/var/lib/postgresql/data
      - ./infra/production/init.sql:/docker-entrypoint-initdb.d/01-services.sql:ro
    networks: [backend]
    secrets:
      - postgres_password
      - identity_password
      - identity_migrator_password
      - wallet_password
      - wallet_migrator_password
      - market_catalog_password
      - market_catalog_migrator_password
      - settlement_password
      - settlement_migrator_password
      - branding_password
      - branding_migrator_password
    healthcheck:
      test: [CMD-SHELL, "pg_isready -U postgres -d ravex"]
      interval: 2s
      timeout: 3s
      retries: 30

  identity: &identity
    build:
      context: .
      dockerfile: services/identity/Dockerfile
    read_only: true
    tmpfs: [/tmp]
    cap_drop: [ALL]
    security_opt: ["no-new-privileges:true"]
    networks: [backend]
    environment:
      DATABASE_MODE: runtime
      DOTNET_ENVIRONMENT: Production
      JWT_SIGNING_KEY_FILE: /run/secrets/jwt_signing_key
      IDENTITY_DB_CONNECTION_FILE: /run/secrets/identity_connection
    secrets:
      - jwt_signing_key
      - identity_connection
    depends_on:
      identity-migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy

  wallet-ledger: &wallet-ledger
    build:
      context: .
      dockerfile: services/wallet-ledger/Dockerfile
    read_only: true
    tmpfs: [/tmp]
    cap_drop: [ALL]
    security_opt: ["no-new-privileges:true"]
    networks: [backend]
    environment:
      DATABASE_MODE: runtime
      DOTNET_ENVIRONMENT: Production
      JWT_SIGNING_KEY_FILE: /run/secrets/jwt_signing_key
      WALLET_DB_CONNECTION_FILE: /run/secrets/wallet_connection
      INTERNAL_SERVICE_KEY_FILE: /run/secrets/internal_service_key
    secrets:
      - jwt_signing_key
      - wallet_connection
      - internal_service_key
    depends_on:
      wallet-ledger-migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy

  market-catalog: &market-catalog
    build:
      context: .
      dockerfile: services/market-catalog/Dockerfile
    read_only: true
    tmpfs: [/tmp]
    cap_drop: [ALL]
    security_opt: ["no-new-privileges:true"]
    networks: [backend]
    environment:
      DATABASE_MODE: runtime
      DOTNET_ENVIRONMENT: Production
      JWT_SIGNING_KEY_FILE: /run/secrets/jwt_signing_key
      MARKET_CATALOG_DB_CONNECTION_FILE: /run/secrets/market_catalog_connection
      INTERNAL_SERVICE_KEY_FILE: /run/secrets/internal_service_key
      SETTLEMENT_SERVICE_URL: http://settlement-engine:8080
    secrets:
      - jwt_signing_key
      - market_catalog_connection
      - internal_service_key
    depends_on:
      market-catalog-migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy

  settlement-engine: &settlement-engine
    build:
      context: ./services/settlement-engine
    read_only: true
    tmpfs: [/tmp]
    cap_drop: [ALL]
    security_opt: ["no-new-privileges:true"]
    networks: [backend]
    environment:
      DATABASE_MODE: runtime
      APP_ENV: Production
      SETTLEMENT_DB_CONNECTION_FILE: /run/secrets/settlement_connection
      INTERNAL_SERVICE_KEY_FILE: /run/secrets/internal_service_key
      WALLET_SERVICE_URL: http://wallet-ledger:8080
      MARKET_CATALOG_SERVICE_URL: http://market-catalog:8080
      IDENTITY_SERVICE_URL: http://identity:8080
    secrets:
      - settlement_connection
      - internal_service_key
    depends_on:
      settlement-engine-migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy

  branding: &branding
    build:
      context: .
      dockerfile: services/branding/Dockerfile
    read_only: true
    tmpfs: [/tmp]
    cap_drop: [ALL]
    security_opt: ["no-new-privileges:true"]
    networks: [backend]
    environment:
      DATABASE_MODE: runtime
      DOTNET_ENVIRONMENT: Production
      JWT_SIGNING_KEY_FILE: /run/secrets/jwt_signing_key
      BRANDING_DB_CONNECTION_FILE: /run/secrets/branding_connection
    secrets:
      - jwt_signing_key
      - branding_connection
    depends_on:
      branding-migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy

  identity-migrate:
    <<: *identity
    restart: "no"
    environment:
      DOTNET_ENVIRONMENT: Production
      DATABASE_MODE: migrate
      IDENTITY_DB_CONNECTION_FILE: /run/secrets/identity_migrator_connection
      ADMIN_BOOTSTRAP_EMAIL: ${ADMIN_BOOTSTRAP_EMAIL:?Set ADMIN_BOOTSTRAP_EMAIL}
      ADMIN_BOOTSTRAP_PASSWORD_FILE: /run/secrets/admin_bootstrap_password
    secrets:
      - identity_migrator_connection
      - admin_bootstrap_password
    depends_on:
      postgres:
        condition: service_healthy

  wallet-ledger-migrate:
    <<: *wallet-ledger
    restart: "no"
    environment:
      DOTNET_ENVIRONMENT: Production
      DATABASE_MODE: migrate
      WALLET_DB_CONNECTION_FILE: /run/secrets/wallet_migrator_connection
    secrets:
      - wallet_migrator_connection
    depends_on:
      postgres:
        condition: service_healthy

  market-catalog-migrate:
    <<: *market-catalog
    restart: "no"
    environment:
      DOTNET_ENVIRONMENT: Production
      DATABASE_MODE: migrate
      MARKET_CATALOG_DB_CONNECTION_FILE: /run/secrets/market_catalog_migrator_connection
    secrets:
      - market_catalog_migrator_connection
    depends_on:
      postgres:
        condition: service_healthy

  settlement-engine-migrate:
    <<: *settlement-engine
    restart: "no"
    environment:
      APP_ENV: Production
      DATABASE_MODE: migrate
      SETTLEMENT_DB_CONNECTION_FILE: /run/secrets/settlement_migrator_connection
    secrets:
      - settlement_migrator_connection
    depends_on:
      postgres:
        condition: service_healthy

  branding-migrate:
    <<: *branding
    restart: "no"
    environment:
      DOTNET_ENVIRONMENT: Production
      DATABASE_MODE: migrate
      BRANDING_DB_CONNECTION_FILE: /run/secrets/branding_migrator_connection
    secrets:
      - branding_migrator_connection
    depends_on:
      postgres:
        condition: service_healthy

  gateway:
    build:
      context: .
      dockerfile: services/gateway/Dockerfile
    read_only: true
    tmpfs: [/tmp]
    cap_drop: [ALL]
    security_opt: ["no-new-privileges:true"]
    networks: [backend, edge]
    environment:
      DOTNET_ENVIRONMENT: Production
      JWT_SIGNING_KEY_FILE: /run/secrets/jwt_signing_key
      Cors__AllowedOrigins__0: ${PLATFORM_ORIGIN:?Set PLATFORM_ORIGIN}
      Cors__AllowedOrigins__1: ${ADMIN_ORIGIN:?Set ADMIN_ORIGIN}
    ports:
      - "127.0.0.1:${GATEWAY_PORT:-5100}:8080"
    secrets:
      - jwt_signing_key
    depends_on:
      postgres:
        condition: service_healthy

networks:
  backend:
    internal: true
  edge: {}
volumes:
  production_postgres:
secrets:
  postgres_password:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/postgres_password
  jwt_signing_key:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/jwt_signing_key
  internal_service_key:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/internal_service_key
  admin_bootstrap_password:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/admin_bootstrap_password
  identity_password:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/identity_password
  identity_connection:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/identity_connection
  wallet_password:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/wallet_password
  wallet_connection:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/wallet_connection
  market_catalog_password:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/market_catalog_password
  market_catalog_connection:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/market_catalog_connection
  settlement_password:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/settlement_password
  settlement_connection:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/settlement_connection
  branding_password:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/branding_password
  branding_connection:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/branding_connection
  identity_migrator_password:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/identity_migrator_password
  identity_migrator_connection:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/identity_migrator_connection
  wallet_migrator_password:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/wallet_migrator_password
  wallet_migrator_connection:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/wallet_migrator_connection
  market_catalog_migrator_password:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/market_catalog_migrator_password
  market_catalog_migrator_connection:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/market_catalog_migrator_connection
  settlement_migrator_password:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/settlement_migrator_password
  settlement_migrator_connection:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/settlement_migrator_connection
  branding_migrator_password:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/branding_migrator_password
  branding_migrator_connection:
    file: ${RAVEX_SECRETS_DIR:?Set RAVEX_SECRETS_DIR}/branding_migrator_connection
